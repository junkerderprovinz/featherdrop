A one-line polish to the container's startup log.

## Changed

- **Init-log subtitle now matches the in-app copy.** The banner printed at
  container startup reads "Encrypted at rest, auto-deleted when the link
  expires", in line with the UI subtitle (`docker-entrypoint.sh`).
