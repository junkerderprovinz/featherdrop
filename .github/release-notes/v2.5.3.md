More UI polish.

## Changed

- **Centered brand.** The "featherdrop" logo + wordmark in the header is now
  centered, with the light/dark and language controls floating top-right, so the
  start screen reads more evenly (`app/page.tsx`).
